<h2>Editing Book</h2>
<%= form_for(@detail) do |f| %>
<%# ここからエラーメッセージ %>
  <% if @detail.errors.any? %>
    <div class="error_explanation">
      <h2><%= pluralize(@detail.errors.count, "error") %> prohibited this product from being saved:</h2>
      <ul>
        <% @detail.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%# ここまでエラーメッセージ %>
  <p>Title</p>
  <%= f.text_field :title %>
  <p>Body</p>
  <%= f.text_area :body %><br>
  <%= f.submit 'Update Book' %>
  <% end %>
  <div class="btn"><%= link_to "Show", book_path(@detail.id) %></div>
  <div class="btn"><%= link_to "Back", books_path %></div>